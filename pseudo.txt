 This is javascript exercise rock-paper-scissors game.

 Main functionality:
    Define 'getComputerChoice' method () {
            save floored output of math.random() * 3 as 'choice' variable;
            if output == 0:
                return 'rock';
            elif output == 1:
                return 'paper';
            elif output == 2:
                return 'scissor';
            else:
                log error string message;
                return 'null';
    }

    Define 'playRound' Method (userChoice, computerChoice) {
            if lowercase userChoice == computerChoice:
                return 'Draw!';
            elif lowercase userChoice == 'rock' and computerChoice == 'paper':
                return 'You lost!';
            elif lowercase userChoice == 'rock' and computerChoice == 'scissor':
                return 'You won!';
            elif lowercase userChoice == 'paper' and computerChoice == 'rock':
                return 'You won!';
            elif lowercase userChoice == 'paper' and computerChoice == 'scissor':
                return 'You lost!'; 
            elif lowercase userChoice == 'scissor' and computerChoice == 'rock':
                return 'You lost!';
            elif lowercase userChoice == 'scissor' and computerChoice == 'paper':
                return 'You won!';
            else:
                log error string to console;
                return 'null';
    }

    Define checkUserChoice method (userChoice) {
        if lowercase userChoice == 'rock' or 'paper' or 'scissor':
            return true;
        else:
            return false;
    }

    Define 'game' method () {
        Loop the algorithm where defined variable value is less than or equal to 5 {
            Ask the user for their input ('Rock, Paper, Scissor') and save it as 'userChoice';
            if checkUserChoice(userChoice) == true {
                save getComputerChoice method output as 'computerChoice' variable;
                alert 'playRound' method result;
            } else {
                alert the user about incorrect input.
            }
        }
    }

    prompt the user for round quantity and save output as number in 'roundCount' variable;
    game();